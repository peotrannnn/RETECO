# data (tải riêng qua huggingface_hub, không commit)
reteco_data/
RETECO/
cache/

# run output chi tiết (tái tạo được bằng cách chạy lại, chỉ giữ summary json)
runs/**/*.txt
runs/**/*.trec

# scratch/test runs -- never official, never tracked
runs/scratch/

# leftovers from early manual testing, before src/track1a existed --
# not part of the tracked pipeline output
runs/my_system/
runs/starter/

# python
__pycache__/
*.pyc
*.pyo
.ipynb_checkpoints/

# os / editor
.DS_Store
.vscode/

# secrets
.env
*.token
