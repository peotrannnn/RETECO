# Dependencies for src/track1a.
#
# --method bm25 needs only numpy + scipy.
# --method dense / hybrid and --rerank need the rest.
numpy>=1.24
scipy>=1.10
sentence-transformers>=3.0
torch>=2.2
faiss-cpu>=1.8
