# Dependencies for src/track1a.
#
# --method bm25 needs only numpy + scipy.
# --method dense / hybrid need sentence-transformers (bi-encoder).
# --rerank needs transformers directly (see reranker.py for why it does not
# use sentence_transformers.CrossEncoder).
numpy>=1.24
scipy>=1.10
sentence-transformers>=3.0
transformers>=4.40
torch>=2.2
faiss-cpu>=1.8
